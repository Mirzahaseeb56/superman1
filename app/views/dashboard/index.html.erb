<div class="container">
<% if admin_signed_in? && current_admin.super_admin? %>
<%= link_to 'roles', roles_path, class: 'btn btn-info' %>
<%= link_to 'staff users', staff_users_path, class: 'btn btn-info' %>
<% end %>


<% if admin_signed_in? && !current_admin.super_admin?  %>
<%= link_to 'Update Password',edit_admin_registration_path, class: ' btn btn-info' %>
<%= button_to 'delete account',registration_path(current_admin), method: :delete, class: ' btn btn-danger' %>
<%= link_to 'Update Profile',edit_customer_path, class: ' btn btn-info' %>
<% end %>
</div>
